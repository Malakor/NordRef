
<h1>Nutzer Auslesen</h1>
<%= link_to 'Neuen Benutzer anlegen', new_referee_path %>
<%= link_to 'Neuen Verein anlegen', new_club_path %>
<%= link_to 'Vereine', clubs_path %>
<%= link_to 'Schiedsrichter', referees_path %>

<table>
<tr>
<th>ID:</th>
<th>Vorname</th>
<th>Name</th>
<th>Mail</th>
<th>Verein</th>
<th>Bearbeiten</th>
<th>Anzeigen</th>
<th></th>
</tr>

<% @referees.each do |referee| %>
<% club = Club.find(referee.club_id) %>
<tr>
<td><%= referee.id %></td>
<td><%= referee.firstname %></td>
<td><%= referee.lastname %></td>
<td><%= referee.mail %></td>
<td><%= club.shortform %></td>
<td><%= link_to 'be', edit_referee_path(referee) %></td>
<td><%= link_to 'disp', referee_path(referee) %></td>
<% end %>
</table>

